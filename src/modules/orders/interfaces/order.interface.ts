export interface DeliveryDetails {
  email: string;
  name: string;
  address: string;
  city: string;
}

export interface CartItems {
  name: string;
  image: string;
  price: number;
  quantity: number;
}
